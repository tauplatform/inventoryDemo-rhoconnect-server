build:
  docker:
    web: Dockerfile
    worker: Dockerfile
run:
  web:
    command:
      - export REDISTOGO_URL=$REDISCLOUD_URL && bundle exec thin start -p $PORT -e production
  worker:
    command:
      - export REDISTOGO_URL=$REDISCLOUD_URL && QUEUE=* rake resque:work
